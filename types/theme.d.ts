export type Theme = 'claro' | 'escuro';

export type ThemeContextType = {
  theme: Theme;
  changeTheme: (theme: Theme) => void;

};

export type Theme = 'claro' | 'escuro';

export type ThemeContextType = {
  theme: Theme;
  changeTheme: (theme: Theme) => void;
};
  